# 貓空纜車調度系統

貓空纜車車廂即時調度管理工具。以 Next.js + Supabase 建置，支援多人即時同步、拖曳排班、維修標記等功能。

**線上網址：** https://maokong-dispatch.vercel.app

---

## 系統架構

| 層次 | 技術 |
|------|------|
| 前端框架 | Next.js 16 (App Router) |
| 資料庫 | Supabase (PostgreSQL + Realtime) |
| 部署 | Vercel |
| 樣式 | Tailwind CSS + shadcn/ui |
| 拖曳 | dnd-kit |

**⚠️ 部署注意事項：本專案未接 GitHub 自動部署！**
`git push` 只會把程式碼更新到 GitHub，不會讓線上網址跟著更新。每次改完程式碼、commit + push 之後，一定要另外手動執行：

```
vercel --prod
```

才會真正部署到 https://maokong-dispatch.vercel.app 。（2026-06-03 之後曾發生過連續 6 次 commit 都忘記部署，線上版本停留一個月沒更新的狀況，之後務必養成習慣。）

---

## 帳號與權限

| 角色 | 功能 |
|------|------|
| admin | 完整操作（拖曳、修改、設定、診斷） |
| guest | 唯讀（看即時狀態、搜尋） |

帳號由 admin 在系統設定 → 帳號管理新增。登入後右上角顯示目前角色與帳號 email。

---

## 車廂種類與狀態

### 車廂種類

| 車號 | 類型 | 說明 |
|------|------|------|
| 1–147 | regular | 一般車廂 |
| M1、M2 | maintenance_vehicle | 維修車 |
| 雙握 | shuangwo | 雙握車（固定 1 台）|
| 洞 | dong | 洞車（固定 1 台）|

全系統共 **151 台**車廂（1–147 + M1 + M2 + 雙握 + 洞）。

### 車廂狀態與顏色

| 顏色 | 狀態 | 說明 |
|------|------|------|
| 藍色 | 可用（一般車） | 正常運行 |
| 黃色 | 可用（水晶車） | 特殊車型，由設定指定 |
| 灰色 | M1/M2/雙握/洞 | 固定特殊車廂 |
| 紅色 | 不可用 | 暫時停駛 |
| 黑色 | 報廢 | 封鎖上正線，集中停用區顯示 |
| 橘色 | 管控 | 特別管控狀態 |
| 紫色邊框 | 有維修需求 | 由維修排程區登錄後自動顯示，以 inset box-shadow 呈現，不蓋底色（v1.23） |
| 綠色 | 其他 | 自定義用途 |

狀態顏色（除雙握、洞外）可在系統設定自訂。

---

## 功能總覽

### 頂部工具列（TopBar）

#### 搜尋車號
在頂列輸入框輸入車號，按 Enter 或「搜尋」鍵查詢：
- **單車查詢**：輸入單一車號，顯示所在區域與格位（正線顯示第幾格）
- **多車查詢**（v1.15 新增）：輸入多個車號以空格隔開（如 `45 68 137`），逐一列出每台位置，找不到的標示「找不到」
- 若有維修需求一併標示 ⚠
- 搜尋命中後，所有目標車廂標籤同時套用紅色閃爍外框，持續顯示直到清空搜尋框（v1.1 單車；v1.15 支援多車同時 highlight）
- 正線格位以 `mainLine.positions.indexOf()` 直接查詢，不依賴可能過時的 `location_slot` 欄位（v1.4 改善）

#### 車廂備註全覽（📋 N 按鈕）（v1.4 新增）
頂列 📋 按鈕旁顯示橘色數字（N = 有備註車廂數）：
- 點開後以 Modal 列出所有有備註車廂的 ID、所在位置與備註內容
- Admin 可直接點 🗑 從 Modal 刪除任一筆備註

#### 班次快照（💾 儲存班次）（Admin 限定）
點擊後輸入班次說明，系統記錄當下完整狀態，包含正線排列、各區車廂（含備註）、自定義區（含備註）、維修排程、週排程。**佈告欄不納入快照**（交接內容應由控制員手動維護）。

#### 班次記錄（📂 記錄）（v1.9 新增，Admin 限定）
點擊後從右側滑出班次記錄 Panel，顯示最近 30 筆快照（依台北日期分組，最新在上）：
- **儲存當前狀態**（v1.12 新增）：Panel 頂部輸入名稱後點「儲存」，立即建立手動快照並存入 Supabase `snapshots` 表，儲存後清單自動更新
- 每筆顯示：班次說明、時間、操作者、正線模式與已排台數
- **預覽**：展開摘要，含轉角二站/貓空站台數、維修排程人數、報廢車數
- **還原**：需輸入登入密碼驗證身份，確認後覆蓋正線、車廂、自定義區、維修排程、週排程，完成後自動重新載入；佈告欄不還原
- **刪除**（v1.14 新增）：每筆右下角 🗑 按鈕，點後跳出確認 Dialog，確認才執行刪除，刪除後清單即時更新

#### 匯出功能（v1.12 新增）

| 按鈕 | 說明 |
|------|------|
| 📊 Excel | 下載 `.xlsx`，含 5 個工作表：正線排列、車廂狀態、自定義區、維修排程、週排程，檔名自動帶台北時間 |
| 📷 截圖 | 擷取整個調度畫面（含左右儲車區、正線迴圈、中央面板），存為 2× 解析度 `.png`；圖片右下角自動疊加截圖當下的台北時間（格式：`📷 2026/05/31 17:30:25`），截完畫面不留痕跡 |

---

### 正線迴圈

#### 模式切換
支援 **108 車** 與 **130 車** 兩種模式：
- 左上方按鈕切換，Admin 限定
- 108 模式格子較大（約 41×33px），130 模式格子較小（34×28px），兩模式迴圈版面面積相同，切換不跳動（v1.7 新增）
- 排列方向：順時針，Slot 1 從上排最左格起，順時針遞增（v1.23 改版，原為逆時針）
- 切換開關旁狀態列顯示：**上線 N 台｜雙擊格子輸入車號**（v1.13 移除水晶車廂台數顯示，回覆原始總數格式）

#### 自適應縮放（v1.1 新增）
`ResizeObserver` 持續監聽中央容器實際寬高，以 `scale = min(可用高/迴圈高, 可用寬/迴圈寬)` 等比縮放整個迴圈（最小 0.5），筆電自動縮小、桌機自動放大，無需捲動。

#### 方向箭頭（v1.2 改善，v1.12 修正）
- 中央欄左右各有 ↓（藍色，去程）與 ↑（橘色，回程）大字箭頭
- 上排（← ）與下排（→）各有橘紅色方向箭頭，位置依目前模式動態計算上下排中間格，108 與 130 模式均正確顯示（v1.12 修正：原本 108 模式 ← 箭頭因格位編號超出範圍而消失）

#### 拖曳排班
- 從儲車區直接拖曳車廂至正線任意格
- 拖曳至已有車廂的格子時，原車廂自動歸還轉角二站（v1.4 修正，不再產生幽靈車廂）
- 拖曳至迴圈中央大面積自動插入第一個空格

#### 雙擊格子輸入車號（Admin 限定）
雙擊任意格子後輸入車號：
- 清空後車廂移回轉角二站
- 車號不存在 → 顯示警告 Modal，不執行任何寫入
- 車號已在正線、自定義區或週排程任意位置 → 顯示警告並告知確切位置（v1.18 擴大驗證範圍，原本只檢查正線自身）
- 同時更新 `main_line.positions` 與 `cars.location`，不產生重影（v1.4 修正）

#### 108 → 130 切換（v1.9 新增，v1.10 公式修正）
切換時自動依固定公式重排車廂位置：
- **第 1–65 格**：原有車廂不動
- **第 66–87 格**：自動空出（22 個空格，供插入新車）
- **第 88–130 格**：原第 66–108 格的車廂整體後移 22 格

無論原本 108 格是否滿載皆套用此公式，空格跳過不計入。

#### 130 → 108 切換（v1.7，v1.10 改善）
- 線上車 **≤ 108 台**（含 66–87 空格未補滿的情況）：壓縮空格後直接切換，不彈對話框，車廂順序完整保留（v1.10 修正：原本會誤送車廂至儲車區）
- 線上車 **> 108 台**：彈框顯示目前台數與超出台數，提供三個選項：
  - **確認抽出**：輸入起始車號，連續送回超出的 N 台（v1.10 改為動態台數，不再固定 22 台）
  - **略過抽車，直接切換**：位置最後的 N 台自動歸還轉角二站
  - **取消**
- 所有操作含 optimistic update，畫面即時反映（v1.1 修正）
- 抽車使用批次 API（v1.1 改善，原為逐一更新）

---

### 車廂操作（右鍵選單）

在任意車廂標籤上按右鍵可開啟選單：

| 選項 | 說明 |
|------|------|
| ← 移至轉角二站 | 一鍵移回，同時清除原格位；已在轉角二站時停用（v1.1 改善） |
| ★ 設為基準車 / 取消 | 基準車顯示紅色 ★ 加白色光暈，一次只能一台（v1.4 改善顏色）|
| 狀態切換 | 可用 ✅ / 不可用 🔴 / 管控 🟠 / 其他 🟢 / 報廢（含 optimistic update，v1.1 修正即時反應）|
| ✏️ 新增備註 / 編輯備註 | 開啟輸入 Dialog，Ctrl+Enter 快速儲存（v1.4 新增）|

**狀態切換為報廢時**：系統自動從原區域清除格位，並將車廂歸入停用車廂區，無法再被拖曳或移動（v1.4 新增）。

---

### 車廂備註（v1.4 新增）

| 顯示層級 | 說明 |
|----------|------|
| 橘點指示 | 有備註的車廂標籤左上角顯示橘色圓點（純視覺，不需精準對準） |
| 懸停氣泡 | 滑鼠移至**任意有備註的車廂**即顯示，氣泡出現在卡片**正上方**，游標不會遮擋內容（v1.17 改善：原本需對準橘點才觸發，且氣泡位置在游標下方互擋） |
| 備註全覽 | TopBar 📋 N 按鈕，Admin 可在 Modal 刪除備註 |

右鍵 → 編輯備註，開啟 Dialog 後：
- Ctrl+Enter 快速儲存
- 底部另有獨立「🗑️ 刪除備註」按鈕（有備註時才顯示）

---

### 左側欄

左側欄按 ◀ 收折以釋放正線顯示空間，按 ▶ 展開（v1.1 新增）。

#### 轉角二站儲車區
- 無台數上限，標頭顯示在庫台數（v1.20 移除 /40 限制）
- 可收折，收折後仍可接受拖放
- 車廂格子固定 34×28px（v1.5 改善，格子尺寸統一）

#### 貓空站儲車區（v1.2 遷移至左側欄）
- 無台數上限，標頭顯示在庫台數（v1.20 移除 /10 限制）
- 可收折，位於轉角二站正下方，收折後仍可接受拖放

#### 停用車廂區（v1.4 新增）
位於左側欄最底部，專門顯示所有狀態為「報廢」的車廂：
- 報廢車不出現在轉角二站或貓空站儲車區
- 報廢車廂任何移動操作均被封鎖，須先改變狀態才能移動

#### 貓空站維修車活動日（v1.3 新增）
位於貓空站儲車區下方，供記錄維修車活動日期與相關備註：
- 多行 textarea，Enter 正常換行
- 打字後 600ms debounce 自動存檔
- Realtime 即時同步；Admin 可編輯，Guest 唯讀

---

### 中央面板（正線迴圈中央區域）

中央區以固定左右兩欄排列（v1.7 重構）：

| 欄位 | 內容 | 寬度 |
|------|------|------|
| 左欄 | 週排程區（固定） | 296px |
| 右欄 | 自定義區（上）→ 維修排程 A/B/C 與佈告欄同排（中） | 剩餘空間 |

右欄為垂直排列，內容超出時可捲動。

#### 週排程區（已收G4）
- 週一至週五各 **14 格**（v1.7 從 9 格擴充）
- 可拖入車廂或點 + 輸入車號
- 輸入車號時會驗證：車號不存在、已在正線 / 自定義區 / 其他週排程欄位，皆顯示警告並阻擋（v1.18 新增）；清空格子時同步將車廂歸回轉角二站
- 同一車廂拖曳至同週排程其他格時，原格自動清除（v1.1 修正 stale closure bug）

#### 自定義區（原「測試區」，v1.7 改名）
- 預設顯示 6 個，點數字 0–8 控制顯示數量（v1.6 從 6 擴充至 8，v1.8 預設從 3 改為 6）
- 每區 **9 格（3×3）**（v1.11 從 6 格擴充），可自訂名稱
- 可拖入或點 + 輸入車號，雙擊修改
- 輸入車號時會驗證：車號不存在、已在正線 / 其他自定義區 / 週排程任意位置，皆顯示警告並阻擋（v1.18 新增）；清空格子時同步將車廂位置還原為轉角二站
- 備註顯示區域支援多行換行（v1.6 改善）
- 右鍵選單可一鍵移回轉角二站

#### 維修排程 A / B / C
- 三個維修單元**橫向並排**（v1.7 改為橫排；v1.6 曾為垂直堆疊）
- 各可自訂名稱
- 登錄車號後，車廂狀態自動顯示紫色「有維修需求」
- 輸入車號後即時顯示，無需重整頁面
- 車廂顯示**自動排序**（數字由小到大，非數字車號排後方，v1.7 新增）
- 車號以**空心紫色虛線框**顯示（v1.21 新增），區別於正線/週排程的實心色框，強調「標記用途，非實際排入列車」
- × 刪除按鈕顯示於格子右側

#### 佈告欄（v1.2 新增）
供控制員記錄班次交接資訊：
- 標題可自訂（點 ✏ 修改，預設「佈告欄」）
- 交接事項 textarea，打字後 600ms 自動 debounce 存檔
- Realtime 即時同步——多台電腦同時開啟時，任一端修改立即反映
- Admin 可編輯，Guest 唯讀
- **字級可調**（10–22px，每次 ±1，存入 localStorage，每台電腦獨立記憶，v1.7 新增）
- **高度可拖拉調整**（resize: vertical，最小 120px，v1.7 改善）
- **位置**：與維修排程 A/B/C 同一水平，顯示於第三張卡片右側（v1.8 版面調整）

---

### 車廂診斷（🔍 診斷）（Admin 限定）

點 🔍 診斷按鈕，自動掃描（直接查 Supabase，不依賴本地 state，v1.1 改善）：

| 掃描項目 | 說明 |
|----------|------|
| 各區車廂數量統計 | 正線、轉角二站、貓空站、自定義區、週排程、未分配 |
| 合計驗證 | 是否達到 151 台（1–147 + M1 + M2 + 雙握 + 洞）|
| 孤兒車（orphan） | DB 說在某區但 slots 找不到 |
| 幽靈車（ghost） | slots 有車號但 DB 位置不符（正線、週排程、自定義區皆偵測，v1.1 新增週排程/自定義區偵測）|
| 未分配車 | `location` 無效或為 unassigned |
| 重複出現（duplicate） | 同一車號出現在多個格位（v1.1 新增修復邏輯）|

**一鍵修復**：異常車廂批次移回轉角二站，幽靈格位清除。

---

### 系統設定（⚙）（Admin 限定）

| 分頁 | 功能 |
|------|------|
| 帳號管理 | 新增 / 刪除帳號、切換 admin / guest 角色 |
| 水晶車設定 | 點選車號切換水晶車（黃色顯示）|
| 報廢車管理 | 設定後強制封鎖上正線 |
| 狀態顏色 | 自訂各狀態顏色；顏色儲存後即時反映，不依賴 Realtime（v1.4 修正）|

---

## 資料庫 Schema 補充

基本表結構見 `supabase/schema.sql`。以下為後續新增的欄位，需在 Supabase SQL Editor 手動執行：

```sql
-- v1.2：佈告欄
CREATE TABLE bulletin_board (
  id integer PRIMARY KEY DEFAULT 1,
  title text NOT NULL DEFAULT '佈告欄',
  date text NOT NULL DEFAULT '',
  content text NOT NULL DEFAULT '',
  updated_at timestamptz NOT NULL DEFAULT now()
);
INSERT INTO bulletin_board (id) VALUES (1);

-- v1.3：貓空站維修車活動日
ALTER TABLE bulletin_board
  ADD COLUMN IF NOT EXISTS maintenance_notes text NOT NULL DEFAULT '';

-- v1.4：車廂備註
ALTER TABLE cars ADD COLUMN IF NOT EXISTS notes text;
```

RLS 設定：
- `bulletin_board`：所有人 SELECT，登入用戶 UPDATE
- `cars.notes`：同 `cars` 資料表現有 RLS

---

## 本機開發

```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev
# → http://localhost:3000

# 建置
npm run build
```

### 環境變數（.env.local）

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

### 資料庫初始化

```bash
# 在 Supabase SQL Editor 依序執行：
supabase/schema.sql   # 建表
supabase/seed.sql     # 初始化 149 台車廂資料（雙握、洞需手動補充或透過腳本）
```

### ⚠️ 本機開發環境注意事項（2026-07-03 記錄）

開發機（Acer Swift SF514-55GT）於 2026-07-03 連續 3 次在本機執行 `npm run dev` 開啟 http://localhost:3000 時發生藍畫面當機（停止代碼 `VIDEO_MEMORY_MANAGEMENT_INTERNAL 0x10e`）。排查結論：

| 因素 | 說明 |
|------|------|
| Turbopack 首次編譯負載 | Next.js 16 的 `next dev` 預設用 Turbopack，開啟頁面當下要一次編譯 dnd-kit、html2canvas、xlsx、supabase-js 等套件，CPU 瞬間衝高 |
| Intel 內顯驅動已無新版可裝 | 本機 Intel Iris Xe 驅動版本 30.0.101.1960（2022/09/20）；經查 Acer 官網此型號（SF514-55GT）「最新版」也是同一版本，官方自 2022 年後未再釋出更新 |
| 雙防毒同時掃描 | 原同時安裝 Windows Defender 與 360 Total Security，即時掃描互相干擾，已排除（見下） |

**已完成的緩解措施：**
- `D:\Claude Code Project` 已加入 360 Total Security 信任清單（白名單）
- Windows Defender 即時防護已停用，僅保留 360 Total Security 單一防毒

**本機測試建議：** Intel 顯卡驅動已無更新可用，建議改用 Webpack 取代 Turbopack 啟動開發伺服器，降低瞬間負載：

```bash
npm run dev -- --webpack
```

---

## 部署

```bash
# 部署至 Vercel（preview）
vercel

# 部署至正式環境
vercel --prod
```

---

## 成本說明

| 服務 | 方案 | 每月費用 | 備註 |
|------|------|----------|------|
| Vercel | Hobby（免費） | $0 | 輕量 SSR，流量遠低於免費額度 |
| Supabase | 免費方案 | $0 | 資料量極小（< 5 MB），並發用戶少 |
| **合計** | | **NT$0** | |

### 免費方案限制提醒

- **Supabase 自動暫停**：免費專案若連續 **7 天無任何請求**，資料庫會自動進入休眠，下次訪問需等約 30 秒喚醒。如需避免此情況，可升級至 Supabase Pro（$25 USD/月）。
- **Vercel**：Hobby 方案禁止商業用途；若未來需要商業部署，需升級至 Pro（$20 USD/月）。

---

## 系統轉交說明

本節說明如何將此系統完整移交給其他維護單位（如公司數發部）。

### 轉交原則

採「**交程式碼，對方重建基礎設施**」方式，雙方各自持有獨立帳號，原開發者的雲端服務於移交完成後可直接刪除。

### 需移交的內容

| 項目 | 說明 |
|------|------|
| 程式碼 | `maokong-dispatch/` 整包資料夾（壓縮或 Git repo） |
| 資料庫建表腳本 | `supabase/schema.sql`、`supabase/seed.sql` |
| 追加 SQL | README「資料庫 Schema 補充」章節的 ALTER TABLE 指令 |
| 本文件 | README.md 或本使用說明 docx |

> **不需要**移交：任何帳號密碼、.env 檔、Supabase/Vercel 帳號存取權。

### 移交步驟（接收方操作）

**Step 1 — 建立 Supabase 專案**
1. 至 [supabase.com](https://supabase.com) 申請帳號，新建 Project
2. 在 SQL Editor 依序執行：`schema.sql` → `seed.sql` → 追加 SQL（共三段 ALTER TABLE）
3. 至 Project Settings → API，記下 `SUPABASE_URL` 與兩組 Key

**Step 2 — 部署至 Vercel**
1. 至 [vercel.com](https://vercel.com) 申請帳號
2. 匯入程式碼（GitHub 或直接上傳），建立新 Project
3. 在 Vercel Project Settings → Environment Variables 填入：
   ```
   NEXT_PUBLIC_SUPABASE_URL=（Step 1 取得）
   NEXT_PUBLIC_SUPABASE_ANON_KEY=（Step 1 取得）
   SUPABASE_SERVICE_ROLE_KEY=（Step 1 取得）
   ```
4. 觸發重新部署，確認網頁可正常開啟

**Step 3 — 建立管理帳號**
1. 開啟部署完成的網頁，使用原始帳號暫時登入
2. 進入系統設定（⚙）→ 帳號管理，新增貴單位的 admin 帳號
3. 確認新帳號可正常登入後，刪除原始測試帳號

**Step 4 — 確認資料完整**
1. 登入後至「🔍 診斷」，確認 151 台車廂全數出現
2. 執行一次搜尋、拖曳、班次快照測試，確認功能正常

### 移交完成後（原開發者操作）

- 刪除原 Supabase Project（含所有資料）
- 刪除原 Vercel Project
- 原 GitHub repo 視需求可刪除或設為 private

---

## 版本歷程

| 版本 | 日期 | 主要改動 |
|------|------|----------|
| v1.0 | 2026-05-28 | 初版上線：正線迴圈、拖曳、儲車區、週排程、維修排程 |
| v1.1 | 2026-05-29 | 搜尋高亮、右鍵選單 Bug 修正、週排程 stale closure、正線自適應縮放、診斷強化 |
| v1.2 | 2026-05-29 | 雙握車/洞車（+2 特殊車廂）、佈告欄、貓空站移至左側欄、正線車號字體加大 |
| v1.3 | 2026-05-29 | 貓空站維修車活動日記事欄、中央區分隔框、佈告欄加大 |
| v1.4 | 2026-05-29 | 停用車廂區、車廂備註系統、手動輸入車號同步修正、拖曳幽靈 Bug、搜尋格位修正、狀態顏色設定修正 |
| v1.5 | 2026-05-29 | 左側欄 UI 精簡（格子尺寸統一 34×28px、標頭縮小、左側欄縮窄） |
| v1.6 | 2026-05-30 | 中央面板可拖動排序、維修排程垂直堆疊、自定義區擴充至 8 組、字體放大、週排程 14 格 |
| v1.7 | 2026-05-30 | 「測試區」改名「自定義區」、中央固定左右兩欄佈局、108 模式格子自動放大、130→108 切換略過機制、佈告欄字級可調/高度可調、維修排程橫排+自動排序 |
| v1.8 | 2026-05-31 | 正線空格數字放大（7px→13px）並加深顏色、佈告欄移至維修排程同排（第三張卡片右側）、儲車區標題字體加大、維修排程 B 卡改 4 格換行／C 卡改 3 格換行、自定義區預設開 6 個 |
| v1.9 | 2026-05-31 | 108→130 切換自動重排（前 65 不動，66–87 空出，後 43 台後移）、班次記錄 Panel（預覽＋密碼驗證還原） |
| v1.10 | 2026-05-31 | 108→130 重排公式修正（44-65→66-87）、130→108 壓縮 bug 修正（未補滿時不再誤送車廂）、抽車台數動態化（超出幾台送回幾台）、搜尋欄加 × 清除鍵、正線空格改灰底白車白底視覺分層 |
| v1.11 | 2026-05-31 | 自定義區每卡 6 格→9 格（3×3）並批次更新 DB、正線狀態列新增水晶車廂在線台數、維修車活動日輸入框字體加大至 16px |
| v1.12 | 2026-05-31 | 修復 108 模式 ← 方向箭頭消失（動態計算上下排中間格位）、班次記錄 Panel 加入手動儲存快照、TopBar 新增 📊 Excel 匯出（5 工作表）與 📷 截圖功能 |
| v1.13 | 2026-05-31 | 移除正線水晶車廂在線台數（狀態列回覆「上線 N 台」）、截圖改用 html-to-image 修復 lab() 顏色解析錯誤、截圖右下角自動加入台北時間浮水印 |
| v1.14 | 2026-05-31 | 班次記錄新增刪除功能（🗑 按鈕 + 確認 Dialog 防誤刪，刪除後清單即時更新） |
| v1.15 | 2026-05-31 | 搜尋支援多車號（空格隔開同時查詢），所有命中車廂同時 highlight；搜尋框加寬並更新 placeholder |
| v1.16 | 2026-05-31 | 正線空格格位數字加大（10px → 12px），小於車廂編號字體（14px） |
| v1.17 | 2026-05-31 | 備註氣泡改由整張車廂 hover 觸發（不需對準橘點），氣泡顯示於卡片正上方，游標不再遮擋內容 |
| v1.18 | 2026-05-31 | 重大 Bug 修正：正線 / 自定義區 / 週排程所有手動輸入車號處加入跨區域重複驗證，輸入已在其他位置的車號時顯示警告並阻擋；同時修正清空格子時未同步更新 cars.location 的問題 |
| v1.19 | 2026-05-31 | 自定義區與維修排程改名按鈕加大（✏️ emoji、text-base、加 padding），改善點擊體驗 |
| v1.20 | 2026-05-31 | 轉角二站儲車區與貓空站儲車區移除台數上限（/40、/10），改顯示純在庫台數 |
| v1.21 | 2026-05-31 | 維修排程車號改為空心紫色虛線框樣式（非實心色框），視覺上區隔「標記」與「排入列車」；週排程區標題改名為「週排程區（已收G4）」 |
| v1.22 | 2026-06-03 | 洞車/雙握車排除正線在線台數統計、收折左側欄時補計停用車廂數、正線格子字體加大 |
| v1.23 | 2026-06-07 | 正線排列方向改為順時針（Slot 1 由上排最左起順時針遞增，原為逆時針）、方向箭頭配合更新；維修排程標記車廂改為紫色 inset 邊框（不蓋水晶車廂底色） |
| v1.24 | 2026-06-07 | Bug 修正：moveCar DB 更新失敗時自動還原正線槽位及週排程，避免車廂進入「位置在正線區但位置異常」的不一致狀態；「移至轉角二站」改為強制清除所有位置（正線、週排程、自定義區），處理資料不一致情況 |
| v1.25 | 2026-06-27 | 正線維修中車號外框改為 outline 樣式（畫在卡片外側，視覺更清楚）；顏色改讀 statusColors.maintenance_needed，設定頁「有維修需求」顏色現可正常套用 |
| v1.26 | 2026-07-03 | 新增「G點校正」功能：正線迴圈疊加顯示 G1~G6 支柱參考點標籤，下方新增校正面板，admin 可輸入車廂經過某 G 點時的車號，系統自動環形位移整條正線陣列校正偏移（130 模式對照表待補）；週排程格數修正為 12 格（配合 v1.21 已收 G4 但格數未同步更新的遺漏） |
| v1.27 | 2026-07-04 | G 點校正面板改為 TopBar 按鈕（📍 車廂位置）點擊彈出 Modal，不再固定佔用正線下方版面空間（原本會壓縮迴圈顯示比例）；Modal 內輸入格加大（44×28px → 64×44px）；正線疊層 G 點標籤字級與框線加大，更易辨識；修正 Supabase Realtime 訂閱在開發模式下因 useEffect 未回傳清除函式而重複訂閱噴錯的問題 |
| v1.28 | 2026-07-04 | 修正正線疊層 G 點標籤左右兩側顯示：G2/G3/G4上坡側、G5下坡側改為直排（第一行英文+數字，上/下、坡、側各佔一行），避免長文字橫排超出畫面被裁切；上排標籤（G4/G3/G2下坡側、G1）原本框線會壓進車號格 3px，修正為與下排一致留 4px 間距；中央迴圈最左側新增 20px 固定留白，供 G5 標籤伸展且不影響迴圈整體縮放比例；修正週排程欄寬度計算遺漏（右框線 2px + 垂直捲軸緩衝），欄寬由 296px 調整為 316px，解決週五欄右框線被截掉的問題 |
| v1.29 | 2026-07-04 | 正線圖外圍新增 70px 緩衝空間（MARGIN），避免 G 點側邊直排多行標籤探出原本方框邊界時被容器裁切；純版面留白調整，不影響任何功能邏輯 |
| v1.30 | 2026-07-04 | 修正 v1.29 遺漏：縮放比例（scale）計算公式忘了把緩衝空間一起算入，導致容器實際尺寸比可視範圍大一圈、被迫捲動才能看到完整迴圈；緩衝空間改為上下（MARGIN_Y=30）左右（MARGIN_X=34）分開設定，取剛好夠 G 點標籤伸出不被裁切的最小值，並讓 scale 計算連同緩衝空間一起等比縮小 |
| v1.31 | 2026-07-04 | 修正 10 個 G 點支柱參考點在 108 車正線陣列上的實際位置（index108）：依現場正確位置改為 G4下坡側=0、G5下坡側=14、G6=29、G5上坡側=44、G4上坡側=57、G3上坡側=65、G2上坡側=73、G1=83、G2下坡側=94、G3下坡側=100；`lib/gPoints.ts` 陣列順序同步依位置排序，僅資料修正，未動校正邏輯 |
| v1.32 | 2026-07-04 | 「有維修需求」紫色外框改回內縮樣式（v1.25 曾改為外側 outline，會疊到旁邊車廂格），改用 `boxShadow: inset` 畫在車廂內側並加粗至 4px，不影響相鄰車號格 |
| v1.33 | 2026-07-04 | 正線內側 4 個方向箭頭圖示改為逆時針指向（位置不變，僅翻轉圖示方向）；純視覺調整，Slot 排列順序、108/130 切換公式、G 點校正皆未變動 |
| v1.34 | 2026-07-04 | 補上 10 個 G 點支柱參考點在 130 車正線陣列上的位置（index130）：G4下坡側=0、G5下坡側=17、G6=35、G5上坡側=53、G4上坡側=69、G3上坡側=79、G2上坡側=88、G1=101、G2下坡側=113、G3下坡側=121；樣式沿用 108 車既有的直排顯示邏輯，無需另外調整 |
| v1.35 | 2026-07-04 | TopBar 按鈕文字由「G點校正」改名為「車廂位置」，更貼近 admin 實際操作情境（輸入車廂經過某 G 點時的車號）；純文字顯示調整，功能與校正邏輯未變動 |
| v1.36 | 2026-07-04 | 移除「車廂位置」Modal 內開頭的操作說明文字（控制員回報車廂經過某支柱位置...），簡化視窗內容 |
| v1.37 | 2026-07-04 | 10 個 G 點參考標籤文字顏色改為橘色 |
| v1.38 | 2026-07-04 | G 點標籤改為灰底白字徽章樣式（先橘字後改白字），移除原本的紫色外框 |
| v1.39 | 2026-07-04 | G 點標籤圖示（📍）移除灰底，僅文字部分保留灰底白字徽章；橫式標籤圖示置於文字左側，直排標籤圖示置於文字上方 |
| v1.40 | 2026-08-02 | 班次記錄防呆機制：Supabase 排程 pg_cron 每日 09:00／00:00（台北時間）自動呼叫 `create_auto_snapshot()`，解決先前長達近一個月無自動快照的空窗；「還原」按鈕僅對最新一期快照啟用，較舊快照鎖住並提示原因；還原流程改為登入密碼驗證通過後，再加一層免輸密碼的純點擊二次確認才會實際執行；`operation_logs` 還原記錄的 detail 補上被還原快照的建立時間，方便日後排查 |
